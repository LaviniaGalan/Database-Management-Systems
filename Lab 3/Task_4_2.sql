USE HospitalCopy

ALTER DATABASE HospitalCopy
SET ALLOW_SNAPSHOT_ISOLATION ON

SET TRANSACTION ISOLATION LEVEL SNAPSHOT

BEGIN TRAN
	UPDATE Manager
	SET Name = Name + 'BB'
	WHERE ManagerID = 3

	COMMIT TRAN

